```slc
@block TASK 2.2_auth_pages
priority: critical
intent: "Build sign-in and sign-up pages using the Better Auth client"
scope: phase-2
depends_on: [arch/auth.auth_module, "1.2_shadcn_ui"]
estimate: 25

content:
  - "Route group app/(auth)/ with a centered card layout (no sidebar)"
  - "(auth)/sign-in/page.tsx: email+password form → authClient.signIn.email → on success router.push('/chat')"
  - "(auth)/sign-up/page.tsx: name+email+password form → authClient.signUp.email → account active immediately → router.push('/chat')"
  - "Inline error states on invalid credentials / duplicate email; loading state on submit"
  - "Cross-links between sign-in and sign-up; email+password only (no OAuth, no reset — CONTEXT non-goals)"

acceptance_criteria:
  - "Sign-up creates an account and lands on /chat"
  - "Sign-in authenticates and lands on /chat"
  - "Errors surfaced inline; no email-verification step"
@end
```

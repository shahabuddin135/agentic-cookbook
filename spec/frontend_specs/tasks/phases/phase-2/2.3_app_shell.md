```slc
@block TASK 2.3_app_shell
priority: high
intent: "Protected (app) layout with persistent Sidebar + Navbar"
scope: phase-2
depends_on: [arch/shell.shell_module, "2.1_proxy_protection"]
estimate: 20

content:
  - "app/(app)/layout.tsx: two-pane shell — left Sidebar (conversation history slot, filled in 4.2), main column with top Navbar"
  - "components/navbar.tsx: app title + user menu (avatar dropdown: profile link, sign out via authClient.signOut → /sign-in)"
  - "components/sidebar.tsx: scaffold with 'New chat' button (→ /chat) and a placeholder list region (history wired in 4.2)"
  - "Responsive: sidebar collapsible on small screens"

acceptance_criteria:
  - "/chat and /profile render inside the shell with Sidebar + Navbar"
  - "Sign out works and redirects to /sign-in"
  - "New chat button routes to /chat"
@end
```

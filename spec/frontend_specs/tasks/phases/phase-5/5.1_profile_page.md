```slc
@block TASK 5.1_profile_page
priority: high
intent: "Profile page — view profile and update name"
scope: phase-5
depends_on: [contract/profile_api.frontend_profile_api, "2.3_app_shell"]
estimate: 15

content:
  - "hooks/use-profile.ts: useProfile() ['profile']; useUpdateProfile() (PUT) → invalidate ['profile']"
  - "app/(app)/profile/page.tsx: show email (read-only) + created_at; editable name field with Save"
  - "Validation: name required (mirror backend 422 on empty); success toast"

acceptance_criteria:
  - "Profile loads name/email/created_at"
  - "Updating name persists and re-renders; empty name rejected"
  - "Email is not editable"
@end
```

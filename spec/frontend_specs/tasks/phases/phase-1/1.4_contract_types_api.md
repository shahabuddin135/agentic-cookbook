```slc
@block TASK 1.4_contract_types_api
priority: critical
intent: "TS types mirrored from backend contract + typed fetch client with Bearer injection"
scope: phase-1
depends_on: [contract/contract_index.frontend_contract_registry, arch/auth.auth_module]
estimate: 20

content:
  - "types/contract.ts: define RecipeCardData, RecipeImage, Conversation, Message, Profile, ConsentRequest/Response — EXACTLY mirroring frontend_specs/contract/* (which mirrors backend). No invented fields."
  - "lib/api.ts: apiFetch(path, opts) wrapper around fetch to NEXT_PUBLIC_API_URL"
  - "apiFetch attaches Authorization: Bearer <jwt> from authClient on every call"
  - "Centralize 401 handling (attempt token refresh, else redirect /sign-in) and 429 surfacing"
  - "Export typed helpers: getConversations, getConversation, deleteConversation, getProfile, updateProfile, postConsent (export/agent-chat use raw fetch for blob/stream)"

acceptance_criteria:
  - "types/contract.ts compiles and matches contract field names/optionality"
  - "apiFetch injects Bearer JWT automatically"
  - "401/429 handled centrally"
  - "Any field mismatch vs backend contract is reported as CONTRACT_MISMATCH, not silently adapted"
@end
```

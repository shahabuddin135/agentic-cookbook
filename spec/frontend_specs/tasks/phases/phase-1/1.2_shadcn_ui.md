```slc
@block TASK 1.2_shadcn_ui
priority: critical
intent: "Install and initialize shadcn/ui with the components the app needs"
scope: phase-1
depends_on: [arch/shell.shell_module]
estimate: 15

content:
  - "Initialize shadcn/ui (Tailwind v4 compatible) via bunx; confirm components.json + lib/utils.ts (cn)"
  - "Add components: button, input, textarea, card, dialog, alert-dialog, skeleton, dropdown-menu, avatar, sonner (toast), scroll-area, badge, separator"
  - "Verify Tailwind v4 globals.css imports and theme tokens render"

acceptance_criteria:
  - "components/ui/* generated and importable via @/components/ui/*"
  - "A sample <Button> renders with Tailwind styles"
  - "cn() utility available in lib/utils.ts"
@end
```

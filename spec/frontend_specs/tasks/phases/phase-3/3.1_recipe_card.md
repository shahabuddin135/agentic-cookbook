```slc
@block TASK 3.1_recipe_card
priority: critical
intent: "RecipeCard display component + skeleton loader"
scope: phase-3
depends_on: [arch/chat.chat_module, contract/agent_api.frontend_agent_api]
estimate: 25

content:
  - "components/recipe-card.tsx: takes RecipeCardData; renders intro message + recipe (title, description, prep/cook time, servings, ingredients[], instructions[], tags[], source_url)"
  - "Image via next/image (remote Pexels host configured in 1.1)"
  - "MANDATORY Pexels attribution: render photographer + link to photographer_url whenever image present (Pexels ToS — non-negotiable)"
  - "components/recipe-card-skeleton.tsx: shadcn Skeleton matching card layout"
  - "source_url shown as external link when present"

acceptance_criteria:
  - "RecipeCard renders all RecipeCardData fields, gracefully handling nulls"
  - "Photographer attribution always visible when an image is shown"
  - "Skeleton matches the card shape for a smooth loading→result transition"
@end
```

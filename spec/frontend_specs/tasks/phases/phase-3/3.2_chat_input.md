```slc
@block TASK 3.2_chat_input
priority: high
intent: "Chat input component using @kokonutui/ai-input-search"
scope: phase-3
depends_on: [arch/chat.chat_module]
estimate: 15

content:
  - "Install @kokonutui/ai-input-search (verify Next 16 / React 19 compatibility; wrap as Client Component if it uses client-only features per Next 16 third-party guidance)"
  - "components/chat-input.tsx ('use client'): controlled input → onSubmit(message)"
  - "Disabled while chat status === 'streaming'; clears on submit"

acceptance_criteria:
  - "Input submits a non-empty message to its onSubmit handler"
  - "Input disables during streaming and re-enables after done/error"
@end
```

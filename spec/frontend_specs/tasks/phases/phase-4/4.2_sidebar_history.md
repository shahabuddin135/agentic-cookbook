```slc
@block TASK 4.2_sidebar_history
priority: high
intent: "Populate the Sidebar with conversation history"
scope: phase-4
depends_on: ["4.1_use_conversations", "2.3_app_shell"]
estimate: 15

content:
  - "Wire components/sidebar.tsx to useConversations()"
  - "List items: title (fallback 'New chat'), link to /chat/{id}, active highlight via pathname"
  - "Per-item delete (AlertDialog confirm → useDeleteConversation)"
  - "Loading skeletons + empty state ('Start your first conversation')"

acceptance_criteria:
  - "Sidebar lists the user's conversations, newest first"
  - "Clicking an item navigates to /chat/{id}; active item highlighted"
  - "Deleting removes it and updates the list"
@end
```

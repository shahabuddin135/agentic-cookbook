# Task 5.5 — Final Integration Verification

```slc
@block TASK task_5_5
priority: high
intent: "Final end-to-end checklist — all 9 endpoints, auth, GDPR, agent, security"
scope: phase-5
depends_on: ["5.1", "5.2", "5.3", "5.4"]
estimate_minutes: 15

content:
  final_main_py_update: |
    # Add profile and consent routers to main.py:
    from app.routers import agent, conversations, profile, consent

    app.include_router(agent.router, prefix="/api")
    app.include_router(conversations.router, prefix="/api")
    app.include_router(profile.router, prefix="/api")
    app.include_router(consent.router, prefix="/api")

  checklist:
    foundation:
      - "[ ] uvicorn starts without errors or warnings"
      - "[ ] MCP servers connect on startup (apify_recipes + pexels_images in logs)"
      - "[ ] GET /health → {status: 'ok', version: '1.0.0'}"
      - "[ ] 4 custom tables in Neon: conversation, message, consent_log, data_deletion_request"

    auth:
      - "[ ] GET /api/conversations without token → 403"
      - "[ ] GET /api/conversations with valid JWT → 200"
      - "[ ] GET /api/profile with valid JWT → user object"
      - "[ ] JWKS endpoint reachable: curl {BETTER_AUTH_URL}/api/auth/jwks"

    agent:
      - "[ ] POST /api/agent/chat → SSE stream with 'recipe' then 'done' events"
      - "[ ] recipe.ingredients is a non-empty list"
      - "[ ] image.photographer is present (Pexels ToS)"
      - "[ ] conversation and both messages saved in DB after chat"

    conversations:
      - "[ ] GET /api/conversations lists created conversations"
      - "[ ] GET /api/conversations/{id} returns conversation + messages"
      - "[ ] DELETE /api/conversations/{id} → 204; subsequent GET → 404"

    profile_gdpr:
      - "[ ] GET /api/profile → {id, name, email, created_at}"
      - "[ ] PUT /api/profile {name: 'Test'} → updated profile"
      - "[ ] GET /api/profile/export → JSON file attachment with all user data"
      - "[ ] DELETE /api/profile → 202 + data_deletion_request row created"
      - "[ ] POST /api/consent {essential: true, analytics: false} → 201"
      - "[ ] consent_log: ip_address_hash is SHA-256 hex, not raw IP"

    security:
      - "[ ] All 9 protected endpoints return 403 without token"
      - "[ ] Rate limit: 11th agent chat request in 60s → 429 with Retry-After"
      - "[ ] OPTIONS /api/agent/chat from localhost:3000 → CORS headers present"
      - "[ ] /docs not accessible when DEBUG=false"
      - "[ ] No raw IP address in consent_log table"

  done_definition: >
    All checklist items pass. Backend is production-ready for integration with
    the Next.js frontend. Proceed to frontend implementation.
@end
```
